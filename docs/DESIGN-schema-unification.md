# Уніфікація схеми `.a7p` через JSON Schema

> Початково задумувалось як "JSON Schema + Quicktype", але Quicktype (генерація типів)
> виявився зайвим — див. виправлення нижче в розділі "Що реально дає Quicktype".
> Реальна цінність — лише в уніфікації валідації.

## Проблема

`profile.clj` — оригінальна Clojure-специфікація (ArcherBC2), що описує обмеження полів
профілю в "людських" одиницях (double + `fraction-digits`, напр. `r-twist` 0.00–100.00 in/turn).

Три репозиторії (`py`, `js`, `dart`) незалежно реалізують той самий набір правил
у трьох різних валідаційних DSL, перерахувавши межі в raw-цілі (wire-формат, ×10^n):

| Репо | Файл | DSL |
|---|---|---|
| py | `src/a7p/yupy_schema.py` | yupy |
| js | `src/validate.ts` | yup |
| dart | `lib/src/a7p_field_constraints.dart` + `a7p_validator.dart` | ручні Dart-класи |

Дублювання вже призвело до дрейфу: `A7pFieldConstraints.zoom` у Dart свідомо відхиляється від
`profile.clj` (0–4 → 1–6, задокументовано як "баг оригіналу"), тоді як інші репо цього фікса
можуть не мати. Кожна майбутня зміна вимагає синхронного ручного патчу в 3 місцях.

Proto-файл (`profedit.proto`) вже є спільним джерелом для *форми* повідомлення (він буквально
скопійований у всі три репо), але не несе інформації про діапазони/довжини/умовні правила —
цим займається кожна мова окремо.

> Сам факт копії `profedit.proto` у трьох місцях був тим самим класом проблеми, що й з
> валідацією (три копії одного джерела правди, ризик розійтися). Винесено на рівень
> `a7p` — див. п.0 у "Наступних кроках".

## Ціль

Одне джерело правди — JSON Schema — для **валідаційних обмежень**, яких немає в
`profedit.proto` (`minimum`/`maximum`/`maxLength`/умовні правила). Валідація виконується
нативним JSON-Schema-валідатором відповідної мови напряму проти цієї ж схеми — замість
ручного портування правил у yup/yupy/Dart-класи.

### Чому НЕ Quicktype (виправлення початкового плану)

Початкова ідея була "JSON Schema + Quicktype": генерувати типи Quicktype-ом з тієї ж схеми,
на додачу до валідації. **Виявилось зайвим**: `profedit.proto` вже й так є єдиним джерелом
правди для *форми* повідомлення і генерує однакові типи в усі три мови (`protoc` →
`profedit_pb2.py`, `ts-proto` → `.ts`, `protoc-dart` → `.pb.dart`) — задовго до цієї роботи.
Quicktype-згенеровані з JSON Schema типи не замінили б їх (інша форма — представлення
JSON-серіалізованого dict'а, не protobuf-класів; інший casing/API), а додали б **третій**,
паралельний набір типів, який довелось би мапити туди-сюди без жодної реальної вигоди.
Тобто вся цінність цього зусилля — саме у валідації; типи вже були розв'язані до нас.

Навіть якби Quicktype використовувався (гіпотетично, без protobuf), він все одно генерує
лише interfaces/classes + (де підтримується) serialization (`fromJson`/`toJson`), і **не**
транслює `minimum`/`maximum`/`maxLength`/умовні `if/then` у код, що виконує перевірки під
час рантайму — ці ключові слова в кращому разі осідають у докстрінгах. Тобто навіть у
теорії Quicktype закриває лише "уніфікацію типів", а не "уніфікацію валідації". Валідацію
в будь-якому разі треба виконувати окремим, стандартним JSON-Schema-валідатором, який читає
схему безпосередньо (без кодогену):

| Мова | Бібліотека-валідатор | Замінює |
|---|---|---|
| TypeScript / JS | `ajv` | `validate.ts` (yup) |
| Python | `fastjsonschema` (не `jsonschema`, див. бенчмарк нижче) | `yupy_schema.py` |
| Dart | `json_schema` (pub.dev) | `a7p_field_constraints.dart` + `a7p_validator.dart` |

### Бенчмарк на реальних даних (484 файли з `a7p-lib/gallery` + тестові фікстури)

Перевірено на практиці (`schema/a7p.schema.json` + ручна перевірка унікальності `mv`, що
документована як не-виражувана в чистому JSON Schema, — без неї недорахунок 52 файлів):

| Валідатор | us/файл | Відносно `yupy` | Результат (invalid/484) |
|---|---|---|---|
| `yupy` (поточний, ручний) | 623 | 1x (базова лінія) | 52 |
| `jsonschema` (pure-Python) | 2702 | **0.23x — у 4.3 рази повільніше** | 52 (з ручним mv-check) |
| `fastjsonschema` (компільований) | 116 | **5.4x швидше** | 52 (з ручним mv-check) |

Усі три дають ідентичний набір з 52 невалідних файлів (усі — з `.unvalidated/`, усі з причини
дубльованих `mv`) — тобто схема з одним невеликим допоміжним чеком повністю відтворює поведінку
поточного `yupy`-валідатора, а `fastjsonschema` ще й у 5+ разів швидший за нього. Голий
`jsonschema`, попри простоту, для Python — гірший вибір, ніж здавалося на етапі дизайну.

Це ключова відмінність від "перенести yup/yupy-схему 1:1" — замість трьох ручних портів
валідаційної логіки лишається один JSON-файл і три тонкі виклики `validate(schema, data)`.

## Структура схеми

```
a7p/
  schema/
    a7p.schema.json       # єдине джерело правди (draft 2020-12)
    generate.sh            # quicktype-виклики для ts/py/dart
    fixtures/
      valid/*.json         # набір валідних payload'ів
      invalid/*.json        # набір невалідних + очікувана помилка/поле
```

`a7p.schema.json` описує `$defs`: `Payload`, `Profile`, `SwitchProps`, `CoefRow`, плюс enum-и
`TwistDir`, `BcType`, `DistanceFrom`. Числові поля використовують `minimum`/`maximum` у
raw-цілих (той самий "wire" масштаб, що й зараз у yupy/yup/Dart — це вже перевірений канонічний
рівень, а не одиниці з `profile.clj`).

### Vendor-розширення (`x-*`)

Стандартний JSON Schema не покриває все, що зараз явно закодовано в Dart-версії
(`A7pFieldRange`) для UI (одиниці виміру, кількість знаків після коми для відображення) чи
кастомну семантику ("unused"-сентинели, унікальність `mv` окрім нуля). Ці метадані виносяться
в `x-`-префіксовані ключі, які валідатори ігнорують, але codegen/UI-шар може читати:

```jsonc
"rTwist": {
  "type": "integer", "minimum": 0, "maximum": 10000,
  "x-fraction-digits": 2, "x-unit": "in/turn"
},
"coefMv": {
  "type": "integer",
  "x-fraction-digits": 1, "x-ui-fraction-digits": 0, "x-unit": "m/s"
}
```

Це прямий еквівалент сьогоднішнього `A7pFieldConstraints` у Dart — але генерований, а не
ручний, і той самий файл читають усі три мови.

### Умовні правила (coefRows залежно від bcType)

JSON Schema `if`/`then` покриває розгалуження G1/G7 (max 5 рядків, `mv` ≤ 30000) проти CUSTOM
(max 200 рядків, `mv`("ma") ≤ 100000):

```jsonc
"if": { "properties": { "bcType": { "const": "CUSTOM" } } },
"then": { "properties": { "coefRows": { "maxItems": 200, "items": { "properties": { "mv": { "maximum": 100000 } } } } } },
"else": { "properties": { "coefRows": { "maxItems": 5,   "items": { "properties": { "mv": { "maximum": 30000  } } } } } }
```

### Що JSON Schema НЕ виражає нативно

- **Унікальність `mv` окрім значення 0** — не покривається стандартним `uniqueItems` (він
  вимагає унікальності всіх елементів). Лишається невеликою, один раз написаною, перевіркою
  на кожній мові (як і зараз) — але оформленою як єдиний документований інваріант
  (`x-unique-except-zero: "mv"` як анотація-документація), а не приховане правило, яке треба
  здогадатися продублювати.
- **"unused"-сентинели** (`cIdx == 255` означає "не використовується", `distance == 0` означає
  "unused") — виражаються через `oneOf`, але семантика ("чому 255/0 особливі") варта
  коментаря/анотації, а не лише схеми.

## Пайплайн генерації

1. `schema/a7p.schema.json` редагується вручну (це і є новий "profile.clj").
2. `generate.sh` викликає `quicktype schema/a7p.schema.json -o ...` для кожного репо/мови,
   кладучи результат у місце, де зараз лежать ручні типи (`js/src/types.ts`,
   аналогічний dataclass/pydantic-файл у `py`, `.dart`-модель у `dart` — якщо
   вирішено не покладатись on protobuf-згенерованих класів для цього шару).
3. Кожен репо у себе просто читає `schema/a7p.schema.json` (або його копію/пакет) і викликає
   `ajv` / `jsonschema` / `json_schema` замість власного yup/yupy/Dart-валідатора.
4. `schema/fixtures/` — спільний набір валідних/невалідних payload'ів, які кожен репо
   під'єднує до свого тестового рантайму (pytest/jest/dart test), гарантуючи, що всі три
   реалізації погоджуються на одних і тих самих edge-cases (у т.ч. регресійний тест на
   випадок `zoom` 0–4 vs 1–6, який зараз розходиться).

**Статус (реалізовано):** `scripts/compile.py --python` — окремий, вужчий за Quicktype
крок: не генерує типи, а компілює сам валідатор (`fastjsonschema.compile_to_code()`) у
`py/src/a7p/_compiled_schema.py`, щоб прибрати ~20ms компіляції схеми на кожен
`import a7p`. Деталі, цифри й порядок регенерації — `schema/README.md`. `--ts`/`--dart` поки
що заглушки (потребують спершу міграції js на ajv і вибору аналога для dart).

## Рішення щодо відкритих питань

1. **Канонічні межі там, де репо вже розійшлися.** `zoom`: канонічний діапазон — **0–6**
   (0 = не налаштовано/дефолт для switch з `distance_from=INDEX`, підтверджено реальними
   профілями `example2.a7p` в обох `py`/`js`; 1–6 — реальні рівні зуму пристрою), а не
   `profile.clj`-івський 0–4 (документований баг оригіналу). Зафіксовано в
   `schema/a7p.schema.json` (`SwitchProps.zoom`).

   Усі виявлені баги-розбіжності (`c_muzzle_velocity`, `b_weight`, `b_length`, `c_zero_w_pitch`
   відсутній у Dart-валідаторі, `bc_cd`/`mv` у js занижені в 10 разів, `zoom` необмежений у
   js) — **виправлено** в `js`/`dart` (див. `x-fixed` в схемі для деталей по
   кожному полю).

   Два питання, що були не просто числовими розбіжностями, а різницею в семантиці
   валідації, тепер мають прийняте рішення (канон = поведінка `profile.clj`/`py`,
   сам `profile.clj` не чіпали):
   - **`c_idx`**: 0–200 або сентинел 255 ("unused"); 201–254 — невизначений проміжок і має
     відхилятись. `js`/`dart` досі приймають весь діапазон 0–255 без винятку —
     **не виправлено** (це зміна логіки валідації, а не константи).
   - **`switches[].distance`**: 0 ("unused") або `[100, 300000]`, **незалежно** від
     `distance_from` — на відміну від `js`/`dart`, які трактують `distance` як індекс
     0–255 при `distance_from=INDEX`. **Не виправлено** там же з тієї ж причини.

   Обидва рішення закодовані як `x-decision` в `schema/a7p.schema.json` і замкнені тестами
   `py/tests/test_schema_validator.py` (`test_switch_c_idx_bounds`,
   `test_switch_distance_bounds_independent_of_distance_from`) — які на відміну від
   старішого `test_yupy_schema.py` перевіряють саме публічний шлях `a7p.validate()`
   (`schema_validator.py`), а не вимкнений з публічного API `yupy_schema.py`.
2. **та 4. Розповсюдження схеми/fixtures.** `a7p` стає монорепо — `py`, `js`,
   `dart` переїжджають усередину як пакети одного репозиторію. Це знімає питання
   копіювання/submodule/npm-залежності: `schema/a7p.schema.json` і `schema/fixtures/`
   лежать в одному дереві поруч з усіма трьома пакетами, кожен referencing їх відносним
   шляхом напряму (як зараз `profedit.proto` дублюється по копії — у монорепо копії більше
   не потрібні, це має бути один файл).
3. **Публічне API кожного пакета бажано не міняти.** Це означає: Quicktype-згенеровані типи
   **не замінюють** існуючі публічні класи/інтерфейси (`types.ts` у js, публічні
   dataclass/типи в py, публічні класи в dart) — щонайменше не в рамках цієї
   уніфікації. Практичний наслідок для архітектури:
   - Схема одразу замінює **валідаційний шар** (yupy/yup/Dart-класи → ajv/jsonschema/json_schema
     проти `schema/a7p.schema.json`) — це внутрішня деталь реалізації, публічного API не
     торкається (сигнатура `validate(payload)` лишається).
   - Quicktype-генерація типів стає **опційним** кроком і, якщо застосовується, генеровані
     типи мають бути 1:1 сумісні з існуючими публічними (ті самі назви полів/enum-значень,
     той самий casing) — або взагалі не виставляються назовні, а лишаються внутрішнім шаром,
     а публічні класи й далі пишуться вручну (чи генеруються з protobuf, як зараз) і просто
     мапляться в/з internal-схема-типу.
   - Тобто зі старту практичний обсяг — це заміна валідації, а не типів; ризикована частина
     (заміна публічних типів) відкладається до окремого рішення, якщо взагалі знадобиться.

## Наступні кроки

Що вже зроблено: схема + `x-decision`/`x-fixed`-анотації, пілот у `py` (fastjsonschema +
jsonschema-fallback, публічний API незмінний, скомпільований валідатор через
`scripts/compile.py --python`), `schema/fixtures/` (7 фікстур), дрібні баги в `js`/`dart`
виправлені, два дизайн-питання (`c_idx`-gap, `switches[].distance`) вирішені й замкнені
тестами в `py`, усі три сабмодулі перейменовані `py`/`js`/`dart`, `profedit.proto` винесено
на рівень `a7p` (п.0 нижче), `dart` мігровано на schema-based валідатор через
`json_schema` (п.1 нижче), `js` мігровано з `yup` на компільований `ajv`-валідатор разом з
першим test suite для `js` (п.2–3 нижче) — усі три мови тепер валідують проти єдиної схеми,
`py`/`js`/`dart` повністю злиті з сабмодулів у монорепо з повною git-історією (п.7 нижче).

Далі, приблизно в такому порядку:

0. ~~**Винести `profedit.proto` на рівень `a7p`.**~~ **Зроблено.** Файл переїхав у
   `proto/profedit.proto` (корінь `a7p`, поруч зі `schema/`); локальні копії прибрані
   з `py/proto/`, `js/src/proto/`, `dart/proto/`. Кодогенові шляхи оновлені: `py` (немає
   власного скрипта — викликати `protoc` напряму з `../proto`), `js` (`package.json`
   `build:proto` тепер `-I=../proto`), `dart` (`bin/generate_proto.dart` тепер `-I ../proto`
   — сам скрипт лишився, бо робить платформо-незалежний пошук плагіна `protoc-gen-dart` у
   pub-cache, чого не варто дублювати в bash). Новий оркестратор `scripts/generate_proto.sh`
   викликає всі три (`--python`/`--ts`/`--dart` або без прапорця — всі разом); деталі —
   `README.md`, розділ "proto/profedit.proto". Зміни зроблені в submodule-репо на гілці
   `fix/schema` (staged, не закомічені — коміти робить користувач вручну).
1. ~~**Мігрувати `dart` на schema-based валідатор.**~~ **Зроблено.** `A7pValidator`
   (`dart/lib/src/a7p_validator.dart`) тепер валідує через пакет `json_schema` (pub.dev)
   замість ручних range-check-методів; публічний API (`A7pValidator.validate(Payload)`,
   `A7pFieldError`, `A7pValidationException`) не змінився. `json_schema` не має
   Quicktype/fastjsonschema-подібного codegen (перевірено — немає готового "compile
   schema → Dart-код" інструменту), тому схема запікається як raw Dart string-константа
   (`dart/lib/src/generated/a7p_schema.g.dart`, генерується `scripts/compile.py --dart`) —
   це, а не Flutter-асети чи `package:`-URI, надійно переживає AOT/web-збірки (деталі й
   чому — `README.md`, розділ `--dart`). `JsonSchema` будується з цієї константи один раз
   (lazy singleton у `A7pValidator._schema`), а не при кожному `validate()`. Unique-`mv`
   перевірка (не виражається в чистому JSON Schema) залишена окремим ручним чеком, як і в
   `py`. Payload (protobuf) конвертується в snake_case `Map` вручну (`_payloadToJson`) —
   dart-протобуф `writeToJsonMap()` дає camelCase, що не збігається зі схемою. Звірено на
   `schema/fixtures/` (усі 7 фікстур дають очікуваний результат, включно з відомим
   `duplicate_mv`-гепом) + новий `dart/test/a7p_validator_test.dart` (9 тестів на реальних
   protobuf-`Payload`, включно з `c_idx`-gap і mv-унікальністю); `dart analyze
   --fatal-infos` і `dart format` чисті. Побічний ефект: стара ручна перевірка
   `c_zero_distance_idx` динамічним `len(distances)-1` (задокументована як
   `x-discrepancy` розбіжність з `py`/`js`) прибрана — тепер `dart` теж використовує
   статичний діапазон 0–255, як і решта.
2. ~~**Мігрувати `js` на `ajv`.**~~ **Зроблено**, разом з п.3 (нижче) — в один прохід,
   оскільки `ajv`, на відміну від `json_schema` в dart, одразу підтримує компільований
   standalone-валідатор, тож не було сенсу спочатку валідувати проти схеми в рантаймі й лише
   потім компілювати окремим кроком. `yup` і `@types/yup` прибрані з `js/package.json`
   повністю (публічний production-залежностей `js` тепер на одну меншою, а не більшою —
   `ajv` лишається лише devDependency, кодогену; скомпільований валідатор не має рантайм
   залежності від `ajv` взагалі). Спершу написаний мінімальний test suite
   (`js/test/validate.test.ts`, 10 тестів — Jest+Babel-інфраструктура вже була в
   `package.json`, лише не була підключена: не було `jest`-пакета й `"test"`-скрипта) на
   основі тих самих категорій, що й `dart/test/a7p_validator_test.dart`, і лише тоді
   замінений валідатор — можна звірити з паритетом.

   При заміні `validate.ts` виявлено й виправлено побічний бонусний баг: старий код
   імпортував `ValidationError` **з `yup`** (`import {..., ValidationError} from 'yup'`),
   а не з власного `errors.ts`, попри те що `index.ts` перевіряв
   `error instanceof ValidationError` проти класу з `errors.ts` — тобто `instanceof`-перевірка
   в `index.ts` ніколи не спрацьовувала для помилок валідації з `validate.ts` (вони
   потрапляли не в `ValidationError`-гілку catch-блоку, а в загальну `Error`-гілку,
   обгорталися в `EncodeError`/`DecodeError` замість `ValidationError`). Новий код імпортує
   `ValidationError` лише з `errors.ts`, тож цей баг зникає сам собою.

   `InvalidBcTypeError` (клас в `errors.ts`) лишений експортованим для сумісності публічного
   API, але новий код його більше не кидає — некоректний `bc_type` тепер просто одна з
   помилок enum-валідації схеми, як і в `py`/`dart`, замість окремого класу винятку
   (той код і раніше був практично недосяжний: `bcType` вже відхилявся на попередньому кроці
   `schema.validateSync`, перш ніж доходило до `switch`/`default`).
3. ~~**Реалізувати `scripts/compile.py --ts`/`--dart`.**~~ **Зроблено обидва** (`--dart` —
   п.1 вище, `--ts` — разом з п.2). `--ts` виявився складнішим, ніж очікувалось із
   формулювання "`ajv compile --standalone`" в оригінальному плані: такого CLI-прапорця не
   існує (`ajv-cli --spec=` підтримує лише draft7/draft2019-09, не наш draft 2020-12) —
   реальний шлях лежить через JS API (`Ajv2020` + `standaloneCode()`), винесений у
   `js/scripts/build_schema_validator.mjs` (окремий Node-скрипт, за тим самим принципом, що
   й `dart/bin/generate_proto.dart` — мовно-специфічна логіка лишається в мовному пакеті,
   `compile.py --ts` лише її викликає через `subprocess`). Другий сюрприз: вихід генерується
   як CommonJS (`.cjs`/`.d.cts`), не ESM, попри те що `js/` — `"type": "module"`: `ajv`-івський
   `code.esm: true` лишає голий `require("ajv/dist/runtime/ucs2length")` у згенерованому коді
   (потрібен для Unicode-aware `maxLength`/`minLength`), що падає під ESM-лоадером Node
   (`require is not defined in ES module scope`) — перевірено емпірично на реальному білді
   (`dist/index.js` → `dist/generated/a7p_schema_validator.cjs`), Node's ESM-лоадер `import`-ить
   `.cjs`-файл без проблем (обгортається як default export), тож `validate.ts` (ESM) підключає
   його без жодних змін у власному module-типі пакета.
4. **Новий CI-воркфлоу для самої схеми** — `.github/workflows/schema.yml` у корені
   `a7p` (окремо від `py`-специфічних `ruff.yml`/`pytest.yml`, бо `schema/` не
   належить жодному з трьох сабмодулів). Тригер: зміни в `schema/**`. Що перевіряє:
   - `schema/a7p.schema.json` — валідний JSON Schema (meta-schema check, напр.
     `jsonschema.Draft202012Validator.check_schema`).
   - Кожен `schema/fixtures/valid/*.json` проходить валідацію; кожен
     `schema/fixtures/invalid/*.json` — падає (використати `py`'s
     `schema_validator`/`fastjsonschema`+`jsonschema`, оскільки `py` вже має робочий
     валідатор проти цієї схеми — не писати окремий).
   - **Дрейф `_compiled_schema.py`**: `python scripts/compile.py --python` і
     `git diff --exit-code py/src/a7p/_compiled_schema.py` — щоб згенерований файл не міг
     розійтися зі схемою непоміченим (зараз це суто ручна дисципліна, див. `README.md`).
5. **Прибрати `yupy`/`yupy_schema.py` з `py`.** Зараз лишені навмисно паралельно
   (їх тестує `test_yupy_schema.py`) як референс/страховка. Видаляти тільки після
   впевненості, що новий валідатор відпрацював у продакшені якийсь час.
6. **Quicktype / генерація типів** — опційний крок (п.3 у "Рішеннях" вище), не
   пріоритетний, оскільки публічні типи кожного пакета вирішено не чіпати. Переглянути
   окремо, якщо колись знадобиться єдине джерело для самих типів, а не лише валідації.
7. ~~**Повне злиття в монорепо.**~~ **Зроблено.** `py`/`js`/`dart` більше не сабмодулі —
   звичайні трековані директорії цього репозиторію, кожна з повною git-історією свого
   старого репо, перенесеною через `git filter-repo --to-subdirectory-filter <pkg>`
   (а не голий `git subtree add`, який дає лише reachable-історію без path-filterable
   `git log -- pkg/file`) + `git merge --allow-unrelated-histories`. Старі теги версій
   (37 шт., з трьох різних, несумісних одна з одною систем версіонування) видалені
   локально — репозиторій тепер має власну єдину систему тегів (`v*`,
   `.github/workflows/release.yml`). Ліцензії при цьому виявились не тривіальним
   питанням: `proto/profedit.proto` дослівно (крім `go_package`-опції) походить з
   `JAremko/ArcherBC2` (LGPL-3.0), а `py` через `JAremko/a7p_transfer_example`
   (GPL-3.0, чий README прямо посилається на `o-murphy/a7p` як на "Project homepage") —
   перевірено емпірично через `gh api`/`gh repo view`, а не вгадано. Тому ліцензії
   лишились роздільними, а не уніфікованими: `py` — GPL-3.0, `js`/`dart` — LGPL-3.0
   (обидва генерують байндинги з того самого LGPL-3.0 proto — `js` до цього був ISC,
   піднятий до LGPL-3.0 для консистентності), корінь репозиторію — теж LGPL-3.0 (бо
   `proto/` там і лежить). Старі репо (`o-murphy/a7p`, `a7p-js`, `a7p-dart`) лишені без
   змін/архівації — публікація йде тепер лише через корінь цього репозиторію. Разом із
   цим: єдиний `CHANGELOG.md` у корені (уся історія трьох пакетів, злита туди вручну —
   версії до монорепо-злиття не мали спільної нумерації, тож кожен заголовок версії має
   лише один `### pkg/`-розділ), `scripts/ci/sync_changelogs.py`/`extract-changelog.sh`
   (патерн з `ob-dump`, адаптований), `py/pyproject.toml` (`setuptools_scm` тепер
   `root = ".."`), і `.github/dependabot.yml` в корені (злитий з трьох per-package
   конфігів, `directory:` тепер вказує на `/py`/`/js`/`/dart`).
8. ~~**Долучити `go` як четвертий пакет.**~~ **Зроблено.** `o-murphy/a7p-go`
   (152 KB, 27 комітів, без тегів/релізів) злито тим самим прийомом, що й п.7 —
   `git filter-repo --to-subdirectory-filter go` + `git merge --allow-unrelated-histories`
   (спершу спробувано голий `git subtree add`, і на практиці підтверджено саме те
   обмеження, що й задокументовано в п.7: історія reachable, але кожен старий коміт
   лишається з путями без `go/`-префікса, тож `git log -- go/a7p/decode.go` не працює;
   передомено через `filter-repo`, де це вже коректно). На відміну від `py`/`js`/`dart`,
   `go` — рівноправний четвертий пакет, який має **ту саму** версію-тег `vX.Y.Z`, а не
   власну — і саме тому `go.mod`/`go.sum` лежать у **корені** репозиторію
   (`module github.com/o-murphy/a7p`), а не в `go/`: версіонування Go-модулів вимагає
   tag-префікса з підпапки (`go/vX.Y.Z`) для будь-якого `go.mod` не в корені репо;
   лише кореневий `go.mod` версіюється голим `vX.Y.Z`-тегом — тим самим, що вже водить
   `py`/`js`/`dart`. Практичний наслідок: на відміну від `js`/`dart` (яких `bump-versions`
   у `release.yml` явно бампає щорелізу), `go` взагалі не потребує кроку бампу версії —
   вона *є* тегом.

   Проєкт мав власну (застарілу) валідацію — вендорена копія `profedit.proto` з
   `buf.validate`-анотаціями + `protovalidate-go`/`go-playground/validator` — замість
   спільної `schema/a7p.schema.json`. Мігровано за тим самим принципом, що й `dart`
   (п.1 вище): еквівалента `fastjsonschema.compile_to_code()`/`ajv standaloneCode()` для
   Go не існує (єдиний проєкт, що це обіцяє, `tfkhsr/jsonschema`, мертвий з 2018 і не
   підтримує draft 2020-12 — перевірено емпірично через `gh api`, а не вгадано), тож
   `scripts/compile.py --go` просто копіює `schema/a7p.schema.json` в
   `go/a7p/generated/a7p_schema.g.json` для нативного `//go:embed` (без raw-string-хаку,
   який був потрібен для Dart). `go/a7p/schema_validator.go` компілює схему один раз
   (lazy singleton, `github.com/santhosh-tekuri/jsonschema/v6`) і валідує payload,
   змаршалений через `protojson.MarshalOptions{UseProtoNames: true, EmitDefaultValues: true}`
   — оскільки власні назви полів/enum'ів proto вже snake_case і збігаються зі схемою
   1:1, це не потребує ручного мапінгу camelCase↔snake_case, на відміну від Dart-івського
   `_payloadToJson`; `EmitDefaultValues` обов'язковий, бо без нього protojson пропускає
   нульові скалярні поля (`zero_x: 0` тощо), а схемні `required`-перевірки очікують їх
   присутності. Публічні `ValidateProto`/`ValidateSpec` лишились (обидві тепер викликають
   ту саму `validateSchema`), внутрішній `protovalidate-go`/`go-playground` код видалено
   повністю (на відміну від `py`, де стара `yupy_schema.py` навмисно лишена як референс —
   п.5 вище; тут старого коду просто не було сенсу лишати, він ніколи не перевіряв
   реальні межі значень, лише довжину масивів).

   Міграція вивела на поверхню реальний, раніше непомічений розрив: стара
   `buf.validate`-анотація на `distances` перевіряла лише `min_items`/`max_items`, і
   ніколи — значення елементів, тож `go/assets/example.a7p` (і `dump.a7p`/`switches.a7p`,
   ті самі дані) містять `distances[0] == 0`, що канонічна схема відхиляє
   (`minimum: 100`) — цей факт задокументовано прямо в `go/tests/a7p_test.go`
   (`TestValidator` тепер очікує саме цю помилку, а не мовчки вимагає чистої валідації).
   `go_package`-опція в `proto/profedit.proto` оновлена на
   `github.com/o-murphy/a7p/go/a7p/profedit` (була успадкована від оригінального
   `jaremko/a7p_transfer_example`); невикористаний `protoc-gen-go-grpc`-кодоген прибрано
   (proto не оголошує жодного `service`). Ліцензія: `go/LICENSE` — **GPL-3.0** (як і
   `py`, з тієї ж причини — успадкована від оригінального `o-murphy/a7p-go`), корінь
   лишається LGPL-3.0.
